#include <iostream>

#define FN "room"

using namespace std;

// Округление
// floor - в меньшую
// ceil - в большую сторону
int main() {
  freopen(FN".in", "r", stdin);
  freopen(FN".out", "r", stdin);
  return 0;
}
